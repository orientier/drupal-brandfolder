{#
/**
 * @file
 * Theme override for a pages in which a Brandfolder Browser is rendered,
 * e.g. in an Entity Browser modal context. Remove as much surrounding markup
 * as possible.
 * Users can opt out of this by configuring Brandfolder module settings.
 *
 * @see brandfolder_theme_suggestions_alter()
 * @see brandfolder_preprocess_page__brandfolder_browser_host_page()
 * @see page.html.twig
 */
#}
{{ page.content }}
